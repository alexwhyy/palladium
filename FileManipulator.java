import java.io.*;

public class FileManipulator {
	private User currentUser;

	public boolean writeDatabase(Object object) {
		if (object instanceof SubscriptionList) {
			String fileName = "alex/SubscriptionList.txt";
			
		} else {
			System.out.println("not subscription detected");
		}
	}

	public boolean createDatabase(Object object) {
		if (object instanceof )
	}
}